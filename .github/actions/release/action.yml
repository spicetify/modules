name: Publish
description: Zip stitched modules, create a GitHub release, and record each artifact in vault.json
runs:
   using: 'composite'
   steps:
      -  name: Build
         shell: bash
         run: |
            set -e
            shopt -s nullglob
            pushd dist
            for DIR in */; do
               DIR="${DIR%/}"
               pushd "${DIR}"
               cp metadata.json "../${DIR}.metadata.json"
               # zip -qr . matches vault.ts pack and includes dotfiles;
               # a shell glob would silently drop them.
               zip -qr "../${DIR}.zip" .
               popd
            done
            popd

      # Validate BEFORE anything is published: a module that would
      # fail vault.ts add (previewless, or a version already recorded)
      # must not leave a published release with no vault update.
      -  name: Validate artifacts against the vault
         shell: bash
         run: |
            set -e
            shopt -s nullglob
            for DIR in dist/*/; do
               DIR="${DIR%/}"
               node scripts/vault.ts add "${DIR}" --zip "${DIR}.zip" --skip-existing --check
            done

      # Zips are not byte-reproducible and the release action replaces
      # same-named assets, so re-uploading an already-published version
      # would desync the bytes from the vault's checksum. The vault on
      # origin/main is ground truth here (it may have moved since the
      # tag), not the tag's copy.
      -  name: Prune already-published artifacts
         shell: bash
         run: |
            set -e
            git fetch origin main
            git show origin/main:vault.json > /tmp/main-vault.json
            node -e '
               const { readFileSync, readdirSync, rmSync } = require("fs");
               const vault = JSON.parse(readFileSync("/tmp/main-vault.json", "utf8"));
               for (const dir of readdirSync("dist", { withFileTypes: true })) {
                  if (!dir.isDirectory()) continue;
                  const meta = JSON.parse(readFileSync(`dist/${dir.name}/metadata.json`, "utf8"));
                  if (vault.modules?.[meta.name]?.v?.[meta.version]) {
                     console.log(`prune ${dir.name}: already in the vault`);
                     rmSync(`dist/${dir.name}.zip`, { force: true });
                     rmSync(`dist/${dir.name}.metadata.json`, { force: true });
                  }
               }
            '

      -  name: Release
         uses: softprops/action-gh-release@v2
         with:
            files: |
               dist/*.zip
               dist/*.metadata.json

      # Record the artifacts on top of origin/main, never by copying
      # the tag's vault.json over it: vault changes that landed on main
      # after the tag (curated edits, another publish) must survive,
      # and --skip-existing makes a re-run idempotent. vault.ts add
      # embeds metadata, updatedAt, and checksums, and enforces the
      # preview requirement.
      -  name: Update vault.json on main
         shell: bash
         env:
            PUSH_BRANCH: main
         run: |
            set -e
            git config --local user.email "$GITHUB_ACTOR@users.noreply.github.com"
            git config --local user.name "$GITHUB_ACTOR"
            git fetch origin "${PUSH_BRANCH}"
            git checkout -b vault-update "origin/${PUSH_BRANCH}"
            shopt -s nullglob
            for DIR in dist/*/; do
               DIR="${DIR%/}"
               ARCHIVE="${DIR}.zip"
               URL="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/${ARCHIVE##*/}"
               node scripts/vault.ts add "${DIR}" --artifact "${URL}" --zip "${ARCHIVE}" --skip-existing
            done
            rm -rf dist
            git add vault.json
            if git diff --cached --quiet; then
               echo "vault unchanged"
               exit 0
            fi
            git commit -m 'Updated vault.json'
            git push origin "HEAD:${PUSH_BRANCH}" || {
               git fetch origin "${PUSH_BRANCH}"
               git rebase "origin/${PUSH_BRANCH}"
               git push origin "HEAD:${PUSH_BRANCH}"
            }
