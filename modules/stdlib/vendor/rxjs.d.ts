/*
 * Types for the prebundled vendor/rxjs.js (the rxjs dev dependency
 * provides them; the bundle carries no declarations of its own).
 */
export { BehaviorSubject, Subject, Subscription } from "rxjs";
